<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Intranet</title>
    <!-- Incluir CSS de Bootstrap -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Estilos existentes */
        .bg-custom {
            background-color: #007bff !important; /* Un tono de azul */
        }
        .list-group-item-action-custom:hover {
            background-color: #0056b3 !important; /* Un tono de azul más oscuro al pasar el mouse */
            color: white;
        }
        .sidebar-heading {
            color: #fff;
        }
        #sidebar-wrapper {
            min-width: 250px; /* Ajustar el ancho del sidebar */
        }
        .container-fluid {
            padding: 2rem;
        }
        
        /* Cambiar el color de texto de los enlaces a blanco */
        .list-group-item-action-custom {
            color: white !important;
        }
    </style>
</head>
<body>

<div class="d-flex" style="height: 100vh;">
    <!-- Sidebar -->
    <div class="bg-custom border-right" id="sidebar-wrapper">
        <div class="sidebar-heading">Intranet</div>
        <div class="list-group list-group-flush">
            <a href="#" class="list-group-item list-group-item-action bg-custom list-group-item-action-custom">Crear Cursos</a>
            <a href="#" class="list-group-item list-group-item-action bg-custom list-group-item-action-custom">Crear Usuarios</a>
            <a href="/cerrar-sesion" class="list-group-item list-group-item-action bg-custom list-group-item-action-custom">Cerrar Sesión</a>
        </div>
    </div>
    <!-- /#sidebar-wrapper -->

    <!-- Page Content -->
<!-- Page Content -->
    <div class="container" style="margin-top: 20px;"> <!-- Añade un margen superior de 20px -->
        <div class="d-flex justify-content-end"> <!-- Alinea el botón a la derecha -->
            <a href="/cursos" class="btn btn-secondary mb-3">Volver</a> <!-- Botón Volver -->
        </div>
        <h2>Crear Nuevo Curso</h2>
        <form action="/crear-curso" method="POST">
            <div class="form-group">
                <label for="tituloCurso">Título del Curso</label>
                <input type="text" class="form-control" id="tituloCurso" name="titulo" required>
            </div>
            <div class="form-group">
                <label for="url_imagen">URL del Imagen</label>
                <input type="url" class="form-control" id="url_imagen" name="url_imagen" required>
            </div>
            
            <div class="form-group">
                <label for="url_video">URL del Video</label>
                <input type="url" class="form-control" id="url_video" name="url_video" required>
            </div>
            <div class="form-group">
                <label for="descripcionCurso">Descripción</label>
                <textarea class="form-control" id="descripcionCurso" name="descripcion" rows="3" required></textarea>
            </div>
            <div class="form-group">
                <label for="url_formulario">Link del Formulario de Evaluación</label>
                <input type="url" class="form-control" id="url_formulario" name="url_formulario" required>
            </div>
            <div class="form-group">
                <label for="url_pdf">Link del Material en PDF</label>
                <input type="url" class="form-control" id="url_pdf" name="url_pdf" required>
            </div>
            <button type="submit" class="btn btn-primary">Crear Curso</button>
        </form>
    </div>
<!-- /#page-content-wrapper -->
</div>

<!-- Incluir JS de Bootstrap -->
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body> 